# src

Pasta reservada para módulos Python reutilizáveis (helpers, serviços, componentes de dados e gráficos).

Atualmente o projeto mantém `app.py` como entrypoint na raiz por compatibilidade direta com Streamlit Cloud/Hugging Face Spaces.
